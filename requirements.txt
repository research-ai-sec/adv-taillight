# Core runtime dependencies for the Vanishing Vehicles attack.
# NOTE: the victim network (SurroundOcc) additionally requires mmcv-full, mmdet and mmdet3d;
# install them following the SurroundOcc README (see README.md "Environment Setup").

# Deep learning
torch>=1.10
torchvision>=0.11

# NumPy / SciPy stack
numpy>=1.20
scipy>=1.7
pandas>=1.3
PyYAML>=5.4

# Image / I/O
opencv-python>=4.5
Pillow>=8.4
matplotlib>=3.4
imageio>=2.9

# CLI & utilities
click>=8.0
tqdm>=4.62
requests>=2.26
dill>=0.3.4
loguru>=0.7

# StyleGAN-XL dependency stack (feature_networks / training)
timm>=0.9
ftfy>=6.1
regex>=2022.1.18
pyspng>=0.1.0

# nuScenes data (dataset infos)
nuscenes-devkit>=1.1
pyquaternion>=0.9.9
